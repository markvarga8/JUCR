query GetUsers($name: String!) {
  search(query: $name, type: USER, first: 100) {
    edges {
      node {
        ... on User {
          id
          name
          bio
          url
        }
      }
    }
  }
}
