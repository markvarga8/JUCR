query myOrgRepos($name: String!) {
  search(query: $name, type: REPOSITORY, first: 10) {
    edges {
      node {
        ... on Repository {
          name
          description
        }
      }
    }
  }
}
